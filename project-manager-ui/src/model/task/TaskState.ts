export enum TaskState {
  TODO = "TODO",
  DOING = "DOING",
  DONE = "DONE",
  REVIEWED = "REVIEWED",
}

export const TaskStateTable = [
  TaskState.TODO,
  TaskState.DOING,
  TaskState.DONE,
  TaskState.REVIEWED,
];
